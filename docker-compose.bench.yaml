# Bench override — generated by tests/perf/bench_matrix.py.
services:
  api:
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 2g
    environment:
      - WORKERS=${WORKERS:-1}
    cpus: 1.0
    mem_limit: 2g

  generation-worker:
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 2g
    cpus: 1.0
    mem_limit: 2g
    environment:
      - GEN_WORKERS=1
