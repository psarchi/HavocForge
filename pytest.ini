[pytest]
testpaths = tests
norecursedirs = _old_for_deletion .venv .git node_modules build dist
markers =
    ci: fast, focused tests for continuous integration
    integration: tests that require running services (API, WebSocket, Redis, Postgres)
    perf: performance / throughput tests
addopts = -m "not integration"
